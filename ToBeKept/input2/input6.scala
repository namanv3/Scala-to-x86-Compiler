object age_check {
		def func(x : Array[Int]) : Int = {

        var age : Int = 20
        if(age > 18){
            println ("Age is greate than 18")
          }
        else{
            println("Age is less than 18")
        }
		}
}
